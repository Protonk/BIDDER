"""
Deferred phase-2 plotter.

Renewal-time histograms need per-walker event tracking that phase 1 does
not record. This file exists so the directory shape is complete, but the
implementation remains intentionally deferred.
"""


def main():
    raise SystemExit(
        'renewal.py is deferred to phase 2; add renewal-gap tracking to '
        'common.py before implementing this plot.'
    )


if __name__ == '__main__':
    main()
